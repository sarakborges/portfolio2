// Dependencies
import React from "react";

// Styles
import { NavBarWrapper, NavBarTitle } from "./style";

// Component NavBar
const NavBar = () => {
  // DOM
  return (
    <NavBarWrapper>
      <NavBarTitle>aaa</NavBarTitle>
    </NavBarWrapper>
  );
};

export default NavBar;
